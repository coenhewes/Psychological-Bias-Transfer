# PBT HANDOFF — full state for new session (2026-07-10, end of day)

This is a complete handoff. Everything below is VERIFIED LIVE STATE, not from memory.
Repo: `/home/forge/Psychological-Bias-Transfer`. Vault: `/home/forge/Obsidian Vault/Projects/Psychological-Bias-Transfer/`.

================================================================
## 1. THE PROJECT (what we are doing)
================================================================
Psychological-Bias-Transfer (PBT) evaluation for a paper. We fine-tune small LLMs on
"distress-corpus" text, then measure whether the trained model shows 4 cognitive-distortion
markers more than a control. We need a valid statistical result + a 2nd independent judge
for inter-rater agreement (kappa).

Validated result REQUIREMENT (user standing order): clean end-to-end run, iterated until
valid, logged for the paper. Scope:
  MODELS = Llama-3.1-8B + Qwen2.5-7B
  SEEDS  = 17, 42, 73, 88, 91  (5)
  JUDGES = Gemini (local, DONE for Llama) + gpt-oss-20b (Vertex, IN PROGRESS)
  ANALYSIS = factorial ANOVA (model × corpus) + paired t-tests + kappa(Gemini vs gpt-oss)
Hardware is explicitly ALLOWED to be heterogeneous (mix L4/T4/A100) — artifacts are
chip-independent. Judge (gpt-oss) MUST stay on Vertex (user override: "NO, we keep the
judge on vertex").

The 4 markers (defined in `evaluation/marker_definitions.py`, present/absent per row):
  rumination, catastrophizing, doom_framing, certainty_collapse

================================================================
## 2. WHAT IS DONE (verified)
================================================================
- Llama-3.1-8B training: all 5 seeds (17,42,73,88,91) trained. Adapters in GCS
  `gs://parallax-model-training-citric-snow-496311-f6/runs/runs/llama3.1-8b_<corpus>_seed<NN>/final_adapter/`
  (corpus = the distress corpus name used; check generation script for exact dir).
- Llama generation: all 5 seeds × treatment/control = 10 `*_fp.jsonl` files (600 rows each)
  in GCS `generations_fp/llama3.1-8b_{treatment,control}_seed{17,42,73,88,91}_fp.jsonl`.
- Gemini judge: all 10 Llama files judged → `eval_outputs/judged_llama/` (10 `*.judged.jsonl`).
- Llama 5-seed paired t-tests: VALID. See
  `results/interim_5seed_llama_2026-07-10.md`:
    rumination       12.15 vs 0.029  p_bonf=8e-6   d=18.8
    catastrophizing   7.11 vs 0.009  p=5.4e-4      d=6.4
    doom_framing      6.29 vs 0.010  p=1.1e-3      d=5.3
    certainty_collapse 5.07 vs 0.21  p=1.2e-3      d=5.3
- Qwen2.5-7B: NOT YET generated/judged (deferred to the full clean run).
- statsmodels + power analysis: installed (per earlier session).

================================================================
## 3. gpt-oss-20b 2ND JUDGE — STATUS: MODEL LOAD VALIDATED, JUDGE CODE BROKEN
================================================================
This is the current blocker. Two sub-problems, one solved, one NOT:

### 3a. MODEL LOAD — SOLVED (PROBE_OK achieved 2026-07-10)
gpt-oss-20b (openai/gpt-oss-20b on HF) is a 20B MoE that ships MXFP4-quantized weights.
The MXFP4 CUDA kernels DO NOT engage in transformers 4.57.6 (triton 3.5.0 present but the
kernel won't load → silently dequantizes to bf16 → OOM on L4 22GB AND A100 40GB).

VALIDATED WORKING LOAD (this is what the probe confirmed with PROBE_OK):
  - Strip `quantization_config` (Mxfp4Config) from the loaded AutoConfig.
  - Load 8-bit BnB: `BitsAndBytesConfig(load_in_8bit=True)`, `device_map="auto"`.
  - Needs A100-40GB (`a2-highgpu-1g`, accel `NVIDIA_TESLA_A100`). ~20GB fits.
  - L4 is NOT viable (22GB too small for 8-bit MoE). A100-80GB NOT available in this project.
  - Install recipe `scripts/_install_transformers.sh`: transformers 4.57.6 (no-deps) +
    torch 2.5.1+cu121 + triton 3.5.0 installed LAST (so nothing downgrades it).
  - The model must be loaded with `trust_remote_code=True`.

PROBE: `scripts/probe_gptoss.py` produces a tiny generation and writes
`PROBE_OK`/`PROBE_FAIL` to `gs://.../generations_fp/probe_gptoss_RESULT.log`.
Last probe that hit PROBE_OK used 8-bit BnB. (Note: the probe script itself was also
patched to 8-bit — see below.)

### 3b. JUDGE CODE — BROKEN (this is what blocks the real output)
`evaluation/judge_gptoss.py` first ran (before fix) and wrote 10 `*.gptoss.judged.jsonl`
files (6000 rows) — BUT EVERY ROW is a crash artifact:
  raw = "ERROR: name 'torch' is not defined"
  present = False  (because score_one() wraps the call in `with torch.no_grad():` and
                     `torch` was only imported INSIDE load_model(), not at module scope)
FIX APPLIED: added `import torch` at module top (line 66). Also changed load_model() to
the validated 8-bit BnB + Mxfp4Config-strip recipe (matches the probe).

After the torch fix, a FULL judge run was submitted (job 1206749754201997312) but was
CANCELLED while still PENDING (we decided to prove the fix on 1 row first per user request).

### 3c. 1-ROW TEST — CURRENTLY IN FLIGHT, NOT YET VERIFIED
User asked: prove the fix on 1 row with the same structure, then do the full run.
Test runner: `scripts/run_judge_gptoss_test.sh` (mirrors run_judge_gptoss.sh but:
  - downloads ONLY `llama3.1-8b_treatment_seed17_fp.jsonl`,
  - `head -1` → one_row.jsonl,
  - runs `python3 evaluation/judge_gptoss.py --generations-dir ... --out-dir ... --max-samples 1`,
  - uploads to `gs://.../generations_fp/TEST_gptoss/`,
  - ALSO `cat`s the judged file into the log + has `set -x` + `REACHED_JUDGE_LINE` marker).

TEST JOB CURRENTLY LIVE: `3315419542229876736` (us-central1, A100, PENDING as of 04:38 UTC).
PRIOR test jobs (4692535865786761216, 6681297313735901184) BOTH SUCCEEDED but their logs
STOPPED at "transformers stack install done" — the judge run output NEVER landed in the log,
and no file appeared in TEST_gptoss/. ROOT CAUSE UNKNOWN — the script silently dies between
the install step and the `python3 judge_gptoss.py` line, with NO error captured. The `cat`
of the result and `REACHED_JUDGE_LINE` marker (added in the 3rd test, job 3315419542229876736)
are the diagnostic to read once that job runs. The log is at
`gs://parallax-model-training-citric-snow-496311-f6/generations_fp/judge_gptoss_test.log`.

KNOWN RECURRING INFRA ISSUES (verify live, don't trust this list blindly):
  - gcloud `ai custom-jobs create` intermittently returns a TRANSIENT NETWORK ERROR with
    empty JID. The driver scripts skip empty-JID pairs; for one-shot jobs, just retry the
    submit (loop 5x with sleep 8 — works).
  - A100 us-central1 allocation sometimes stalls >15 min with NO startTime (vs ~2-3 min
    normal). If PENDING with no startTime past ~25 min, CANCEL + resubmit (proven fix).
  - The background-log-uploader race: worker `exec > /tmp/full.log` + a `( while true; do
    gsutil cp /tmp/full.log ...; sleep 30; done ) &` loop. Sometimes the final judge output
    is lost before upload. Mitigation: `cat` result into log + explicit `gsutil cp` + `sync`.
  - "ModuleNotFoundError: No module named 'pythonjsonlogger'" at worker startup is HARMLESS
    sitecustomize noise — ignore it.

================================================================
## 4. THE kappa TRAP (do not be fooled)
================================================================
`eval_outputs/kappa_gemini_vs_gptoss.json` currently shows kappa=1.000 for all markers.
THIS IS FALSE. It is an artifact: gpt-oss predicted present=0 for ALL 6000 rows (constant
predictor), so with one constant judge, Cohen's kappa degenerates to 1.0 mathematically.
REAL kappa can ONLY be computed after the judge code fix produces genuine verdicts.
The kappa computation script logic (per-marker binary present/absent, pooled overall) is
correct and saved in `eval_outputs/kappa_gemini_vs_gptoss.json` generation step — recompute
after real gpt-oss output exists. Gemini positives: rumination 1804, catastrophizing 1051,
doom_framing 927, certainty_collapse 772 (out of 6000).

================================================================
## 5. SECRETS / CREDENTIALS (where they are, what they are)
================================================================
- GCP PROJECT: citric-snow-496311-f6  (project number 988662010204)
- GCS BUCKET: parallax-model-training-citric-snow-496311-f6
- GCP SA KEY (JSON): /home/forge/.config/forge/gcp/0c44fb1a347e.json
  Embedded into the worker via base64 in the launcher scripts. NEVER print its contents.
- HF_TOKEN: in `/home/forge/Obsidian Vault/Projects/Psychological-Bias-Transfer/.env`
  (and the repo's `.env` is sourced by the runner scripts). Token expires — if a 401
  appears during model download, the token expired; re-source the vault .env.
- GEMINI_API_KEY: also in the vault .env (used by the local Gemini judge `evaluation/judge.py`).
- Vertex region used: us-central1 (us-east1 also works; A100 us-east1 submit hit the
  transient net error more often).
- These are NOT to be written into logs, commit messages, or chat output. Redact as [REDACTED].

================================================================
## 6. KEY FILES (absolute paths)
================================================================
Repo `/home/forge/Psychological-Bias-Transfer/`:
  evaluation/judge_gptoss.py        ← gpt-oss judge (FIXED: import torch + 8-bit load). HAS --max-samples flag.
  evaluation/judge.py               ← Gemini judge (local, DONE).
  evaluation/marker_definitions.py  ← the 4 markers (tarred into worker).
  evaluation/generate_outputs.py    ← generation (device-agnostic, DONE for Llama).
  analysis/statistical_analysis.py  ← paired t-tests / ANOVA.
  scripts/_install_transformers.sh  ← gpt-oss install recipe (triton 3.5.0 LAST).
  scripts/probe_gptoss.py           ← model-load probe (8-bit BnB, PROBE_OK confirmed).
  scripts/run_probe_gptoss.sh       ← probe launcher (static RESULT.log path).
  scripts/drive_probe_gptoss.sh     ← probe driver (rotates region/accel pairs, A100-first).
  scripts/run_judge_gptoss.sh       ← FULL judge launcher (10 files → TEST_gptoss sibling path? NO, real path).
  scripts/run_judge_gptoss_test.sh  ← 1-ROW test launcher (→ TEST_gptoss/). HAS set -x + markers.
  scripts/gpu_job_watchdog.sh       ← watchdog (PENDING>30m / RUNNING>90m → cancel).

Local outputs:
  eval_outputs/judged_llama/         ← 10 Gemini-judged files (REAL, use these).
  eval_outputs/judged_gptoss/        ← 10 gpt-oss files (BROKEN — all-NO, do NOT trust).
  eval_outputs/kappa_gemini_vs_gptoss.json  ← FALSE kappa (see §4).
  eval_outputs/drive_probe.log       ← driver log (may have stale lines).

Vault `/home/forge/Obsidian Vault/Projects/Psychological-Bias-Transfer/`:
  .env                                ← HF_TOKEN + GEMINI_API_KEY.
  IN_FLIGHT.md                        ← this session's live tracking (needs rewrite, see §8).
  results/interim_5seed_llama_2026-07-10.md  ← VALID Llama 5-seed result.
  results/pipeline_validated_procedure.md    ← TRAP log #1-#20 (read for full history).
  results/paired_ttests_5seed.csv, marker_frequencies_5seed.csv.

GCS:
  generations_fp/llama3.1-8b_*_seed*_fp.jsonl        ← 10 raw generations (source for judge).
  generations_fp/*_fp.gptoss.judged.jsonl           ← BROKEN gpt-oss output (all-NO).
  generations_fp/TEST_gptoss/                        ← where 1-row test output goes (empty until test passes).
  generations_fp/probe_gptoss_RESULT.log             ← probe result log.
  generations_fp/judge_gptoss_full.log               ← full judge log.
  generations_fp/judge_gptoss_test.log               ← 1-row test log (READ THIS for job 3315419542229876736).

================================================================
## 7. WHAT TO DO NEXT (in order)
================================================================
1. CHECK TEST JOB 3315419542229876736 (us-central1). Wait until RUNNING, then read
   `gs://.../generations_fp/judge_gptoss_test.log`. Look for:
     - "REACHED_JUDGE_LINE" (proves script reached the python call)
     - "[gptoss] loading..." (model loads)
     - the `cat`'d judged JSON content (should show real present=True/False + raw verdict,
       NOT "ERROR: name 'torch' is not defined")
   If the log STILL stops at "transformers stack install done" with no REACHED_JUDGE_LINE,
   the silent-death bug is BETWEEN install and the judge line — debug by adding explicit
   `echo` after each step (gsutil cp of fp file, head -1, wc -l) and re-run test.

2. VERIFY 1-ROW VERDICT IS REAL. Download TEST_gptoss/*.jsonl, inspect: present should be a
   genuine boolean; raw should be a parsed YES/NO + confidence, not an error string.
   If real → fix PROVEN.

3. IF PROVEN: cancel any in-flight test, then launch the FULL judge:
   `VERTEX_REGION=us-central1 bash scripts/run_judge_gptoss.sh`
   (retry submit up to 5x on transient net error). Wait for 10 `*.gptoss.judged.jsonl` to
   land in generations_fp/ (job uploads at end; then `gsutil -m cp` them to
   eval_outputs/judged_gptoss/ locally, OVERWRITING the broken ones).

4. RECOMPUTE kappa (do NOT trust the existing 1.0). Align Gemini (eval_outputs/judged_llama/)
   vs gpt-oss (eval_outputs/judged_gptoss/) by filename stem (strip _fp and .gptoss/.judged
   suffixes). Compute per-marker + pooled Cohen's kappa on `present` boolean. Expect moderate
   agreement, NOT 1.0.

5. EXECUTE FULL CLEAN RUN for the paper:
   - Generate + judge Qwen2.5-7B (5 seeds, treatment/control) via the same pipeline.
   - Combine Llama + Qwen, 2 judges, run factorial ANOVA (model × corpus) + paired t-tests.
   - Log everything to `results/CLEAN_RUN_LOG.md`.

6. UPDATE results/pipeline_validated_procedure.md with TRAP #21+ (the torch import bug, the
   silent-death-between-install-and-judge bug, the false-kappa trap).

================================================================
## 8. WHAT TO AVOID (hard rules from the user)
================================================================
- NEVER trust "job succeeded" without reading the actual OUTPUT (log content / files written).
  "Succeeds but no output" = a BUG, always root-cause it, never blindly relaunch.
- NEVER treat a stale background-process completion notice as a live event. Verify ACTUAL
  live state first (gcloud describe, gsutil ls).
- Cancel zombie/stuck Vertex jobs instead of leaving them holding GPU slots.
- Clean up as you work: kill dead drivers, cancel stray probes, rewrite IN_FLIGHT.md on every
  state change. Do NOT narrate stale alerts to the user.
- Autonomous execution: do NOT ask for approval to kill/relaunch jobs. Do NOT tell the user
  to "stop checking" — they will keep asking "any update?", just answer with live state.
- Judge (gpt-oss) STAYS on Vertex. Do not move it local.
- At watchdog thresholds, read ACTUAL evidence (startTime / provisioning state), not a blind
  timer. The user explicitly said "30 mins might not be enough — look at the actual evidence
  at the 30 min mark rather than hard rules."
- Keep responses concise, direct, zero padding. The user dislikes verbose/fruity language.
- Redact all secrets. Never print SA key / HF token / Gemini key.

================================================================
## 9. RECURRING FAILURE CHAIN (gpt-oss, for reference)
================================================================
L4 OOM bf16 → A100-40GB OOM bf16 (34GB allocated, fragmentation) → MXFP4 dequant fallback
(triton present but kernel won't engage) → BnB 4-bit rejected (Mxfp4Config conflict) →
triton bumped 3.4.0→3.5.0 + native MXFP4 still dequant → bf16+expandable_segments OOM
(container killed, no log) → 8-bit BnB + strip Mxfp4Config = PROBE_OK (A100-40GB). Then
judge code had its own `torch` NameError bug (constant all-NO output). Now fixing via 1-row
test. The 8-bit load recipe is the validated winner.
